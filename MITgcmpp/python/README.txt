Runs just like the original python scripts by M. B. 
	Specify details of simulation in variables.py
	Specify gif specifications in generateGifs
**note that the variables file now contains parameters for the new plotting routines, so the variables.py file returned from M. B.'s bash scripts (Automated MITgcm scripts) will need to be editted to use these plotting scripts
